# ForgeProof — GitLab Duo Agent Platform configuration
# Docs: https://docs.gitlab.com/user/duo_agent_platform/flows/execution/

image: python:3.11-slim

setup_script:
  - apt-get update -qq && apt-get install -y -qq git nodejs npm > /dev/null 2>&1
  - pip install -q pyyaml
